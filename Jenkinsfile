pipeline {
        agent any
        stages {
                stage('Check if repo exists'){
                        steps{
                                sh 'bash gitcheck.sh'
                        }
